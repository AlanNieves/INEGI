import React from "react";

const Navbar: React.FC = () => (
    <nav className="w-full  text-white py-4 px-8 flex items-center justify-between shadow-md fixed top-0 left-0 z-50">
        <div className="flex items-center gap-2">

        </div>
        <div>
           
        </div>
    </nav>
);

export default Navbar;